function parseString0(inputString0) {
    const splitArray = inputString0.split('~~');
    const lastString = splitArray[splitArray.length - 1];
    const resultArray = lastString.split('-');

    var resultado_0 = resultArray[0]
    return resultado_0;
}

function parseString1(inputString1) {
    const splitArray = inputString1.split('~~');
    const lastString = splitArray[splitArray.length - 1];
    const resultArray = lastString.split('-');

    var resultado_1 = resultArray[1]
    return resultado_1;
}

function parseString2(inputString2) {
    const splitArray = inputString2.split('~~');
    const lastString = splitArray[splitArray.length - 1];
    const resultArray = lastString.split('-');

    var resultado_2 = resultArray[2]
    return resultado_2;
}

function parseString3(inputString3) {
    const splitArray = inputString3.split('~~');
    const lastString = splitArray[splitArray.length - 1];
    const resultArray = lastString.split('-');

    var resultado_3 = resultArray[3]
    return resultado_3;
}

function parseString4(inputString4) {
    const splitArray = inputString4.split('~~');
    const lastString = splitArray[splitArray.length - 1];
    const resultArray = lastString.split('-');

    var resultado_4 = resultArray[4]
    return resultado_4;
}
